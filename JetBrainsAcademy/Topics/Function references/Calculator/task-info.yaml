type: code
files:
- name: src/Main.kt
  visible: true
  text: |-
    fun add(x: Int, y: Int) = x + y

    fun subtract(x: Int, y: Int) = x - y

    fun divide(x: Int, y: Int) = x / y

    fun multiply(x: Int, y: Int) = x * y

    fun calculate(x: Int, y: Int, operator: (Int, Int) -> Int) {
        // write your code here
        val result = ...
        print(result)
    }

    fun main() {
        val x: Int = readLine()!!.toInt()
        val y: Int = readLine()!!.toInt()
        val operator: String = readLine()!!
        when (operator) {
            // write your code here
            "add" -> calculate(...)
            "subtract" -> calculate(...)
            "multiply" -> calculate(...)
            "divide" -> calculate(...)
        }
    }
  learner_created: false
feedback_link: https://hyperskill.org/learn/step/15434#comment
status: Solved
feedback:
  message: <html>Correct solution</html>
  time: "Mon, 28 Feb 2022 15:53:34 UTC"
record: -1
