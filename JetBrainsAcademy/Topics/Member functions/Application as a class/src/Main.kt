class Application(val name: String) {

    // write the run method here
    fun run(s0: String, s1: String, s2: String) {
        println(this.name)
        println(s0)
        println(s1)
        println(s2)
    }
}
